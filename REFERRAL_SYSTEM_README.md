# Реферальная система VPNbot

## Описание

Минимальная реферальная система для отслеживания приглашенных пользователей и их покупок.

## Возможности

1. **Генерация реферальных ссылок** - каждый пользователь получает уникальную ссылку
2. **Отслеживание переходов** - система фиксирует, кто пришел по чьей ссылке
3. **Статистика покупок** - отслеживание, сколько приглашенных совершили покупку
4. **Просмотр статистики** - пользователь может видеть свою реферальную статистику

## Установка

### 1. Применить миграцию базы данных

```bash
python apply_referral_migration.py
```

Эта команда добавит:
- Поле `referrer_id` в таблицу `users`
- Новую таблицу `referrals` для хранения статистики
- Индексы для оптимизации запросов

### 2. Перезапустить бота

После применения миграции перезапустите бота:

```bash
python main.py
```

## Использование

### Для пользователей

1. Открыть бота и нажать кнопку **"🎯 Реферальная программа"**
2. Скопировать свою реферальную ссылку
3. Поделиться ссылкой с друзьями
4. Отслеживать статистику:
   - Сколько человек перешло по ссылке
   - Сколько из них совершили покупку

### Формат реферальной ссылки

```
https://t.me/ВАШ_БОТ?start=refUSER_ID
```

Где `USER_ID` - это Telegram ID пользователя-реферера.

## Структура базы данных

### Таблица `referrals`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Уникальный ID записи |
| referrer_id | BIGINT | ID пользователя, который пригласил |
| referred_id | BIGINT | ID приглашенного пользователя |
| created_at | TIMESTAMP | Дата перехода по ссылке |
| has_purchased | BOOLEAN | Совершил ли покупку |
| first_purchase_at | TIMESTAMP | Дата первой покупки |

### Поле в таблице `users`

| Поле | Тип | Описание |
|------|-----|----------|
| referrer_id | BIGINT | ID пользователя, который пригласил (nullable) |

## API функции

### `create_referral(referrer_id, referred_id)`
Создает запись о реферале при переходе по ссылке.

### `mark_referral_purchased(referred_id)`
Отмечает, что реферал совершил покупку.

### `get_referral_stats(user_id)`
Возвращает статистику рефералов:
- `total_referrals` - общее количество приглашенных
- `purchased_referrals` - количество купивших

### `get_user_referrer(user_id)`
Возвращает ID реферера пользователя.

## Логика работы

1. **Переход по ссылке**: Когда новый пользователь переходит по реферальной ссылке `/start ref123456`, система:
   - Создает нового пользователя
   - Сохраняет `referrer_id = 123456` в таблице `users`
   - Создает запись в таблице `referrals`
   - Уведомляет админов о новом реферале

2. **Первая покупка**: Когда приглашенный пользователь совершает первую покупку:
   - Система автоматически обновляет поле `has_purchased = true`
   - Записывает дату первой покупки в `first_purchase_at`

3. **Просмотр статистики**: Пользователь может в любой момент:
   - Посмотреть свою реферальную ссылку
   - Увидеть количество приглашенных
   - Увидеть количество купивших

## Файлы изменений

### Новые файлы
- `migrations/add_referral_system.sql` - SQL миграция
- `apply_referral_migration.py` - скрипт применения миграции
- `REFERRAL_SYSTEM_README.md` - эта инструкция

### Измененные файлы
- `database/models.py` - добавлена таблица Referrals и поле referrer_id
- `database/db_commands.py` - добавлены функции работы с рефералами
- `handlers/user_handlers.py` - обработка реферальных ссылок и меню
- `keyboards.py` - добавлена кнопка реферальной программы
- `utils.py` - отметка покупок рефералов

## Возможные улучшения

В будущем можно добавить:
- Систему вознаграждений за приглашенных
- Детальную статистику по каждому рефералу
- Экспорт статистики
- Уровни реферальной программы
- Бонусы за определенное количество рефералов

